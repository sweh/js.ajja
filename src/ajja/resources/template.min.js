(function($){"use strict";ajja.template_descriptions={form:'The base `ajja.Form` template',form_field_wrapper:'Wrapper template for each widget. Should contain block for rendering error messages for the widget.',form_boolean:'Template for widgets rendering boolean input fields.',form_multiselect:'Template for widgets rendering multiselect fields',form_number:'Template for widgets rendering number input fields',form_object:'Template for widgets rendering select fields',form_radio_list:'Template for widgets rendering radio list fields',form_string:'Template for widgets rendering text input fields',form_text:'Template for widgets rendering textarea fields.',group:'Template for `ajja.GroupWidget`.',group_item:'Template for an item in a `ajja.GroupWidget`.',list:'Template for `ajja.ListWidget`.',list_item_wrapper:'Wrapper template for each item in a `ajja.ListWidget`.',list_item:'Template for the content of an item in a `ajja.ListWidget`.',list_item_action:'Template for an item action in a `ajja.ListWidget`.',list_item_edit:'Template for edit form (modal dialog) of an item in `ajja.ListWidget`.',table:'Template for `ajja.TableWidget`.',table_head:'Template for head part of a `ajja.TableWidget`.',table_row:'Template for a row of a `ajja.TableWidget`.'};ajja.register_template=function(id,template,description){var html;if(typeof template!=="function"){if(template.indexOf('>')!==-1){html=template;}else if(template.indexOf('#')===0){html=$(template).html();}else if($('#'+template).length===1){html=$('#'+template).html();}else{throw("Can not register template with id '"+id+"'. "+"Don't know how to handle content '"+template+"'.");}
template=Handlebars.compile(html);}
if(description){ajja.template_descriptions[id]=description;}
ajja.templates[id]=template;};ajja.TemplateHandler=Class.$extend({get_template:function(id){if(!ajja.templates[id]){throw("No template found for '"+id+"'. Did you call "+"`form.register_template()`?");}
return ajja.templates[id];},register_template:ajja.register_template,list_templates:function(){var result=[];$.each(ajja.templates,function(id,template){var desc=ajja.template_descriptions[id]||'';result.push({'id':id,'description':desc,'template':template});});return result;}});}(jQuery));
